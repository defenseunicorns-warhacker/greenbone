apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: nginx-certificates-vol
    namespace: greenbone-community-edition
    labels:
        app.kubernetes.io/name: nginx-certificates-vol
        app.kubernetes.io/part-of: greenbone-community-edition
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
